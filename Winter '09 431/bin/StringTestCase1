5 >= 3;
function odd(x) {
  if (x == 0) {
    return false;
  } else {
    return even(x - 1, "fiffle");
  }
}
function even(x) {
  if (x == 0) {
    return true;
  } else {
    return odd(x - 1);
  }
}
print(even(duck.grid));
3 + 4 / ! c . abc * 6 == 5 + 6 + 7 && true || false;