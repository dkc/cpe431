print(even(duck.grid()));
if(1) {2;}
2 * 4 * 5;
function odd(x) {
  if (x == 0) {
    return false;
  } else {
    return even(x - 1, "fiffle");
  }
}
function even(x) {
  if (x == 0) {
    return true;
  } else {
    return odd(x - 1);
  }
}
3 + 4 / ! c . abc * 6 == 5 + 6 + 7 && true || false;